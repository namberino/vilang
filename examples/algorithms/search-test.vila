nhập "lõi:tìm-kiếm";

biến array = [11, 23, 36, 42, 58, 65, 74, 87, 94];
biến length = độ_dài(array);
biến value = 74;

in("Linear search:");
biến lin_val = linear_search(array, value, length);
nếu (lin_val != -1)
{
    in("Element found at index " + lin_val);
}
thì
{
    in("Element not found");
}

in("");
in("Binary search:");
biến bin_val = binary_search(array, value, 0, length - 1);
nếu (bin_val != -1)
{
    in("Element found at index " + bin_val);
}
thì
{
    in("Element not found");
}
