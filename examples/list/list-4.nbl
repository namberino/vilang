hàm test(li)
{
    in(li[0]);
    trả li[0];
}

biến l = test([1, 2, 3]);
in(l);

lớp Test
{
    init(list)
    {
        this.list = list;
    }

    in_list()
    {
        in(this.list[0]);
    }

    trả_list()
    {
        trả this.list[0];
    }
}

biến t = Test([1, 2, 3]);

t.in_list();

biến li = t.trả_list();
in(li);

