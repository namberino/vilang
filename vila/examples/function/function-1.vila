hàm say_hi()
{
    biến n = 10;

    in("Hello world");
    in("This was printed from the say_hi function");

    trả n;
}

biến num = say_hi();

in("This was printed from outside the say_hi function");

say_hi();

in(num);
