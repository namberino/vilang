hàm loop()
{
    biến n = 10;

    khi (đúng)
    {
        nếu (n <= 2)
        {
            dừng;
        }

        in(n);
        n -= 1;
    }

    trả n;
}

biến n = loop();

in("Output of the function: " + n);
