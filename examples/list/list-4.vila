hàm test(li)
{
    in(li[0]);
    trả li[0];
}

biến l = test([1, 2, 3]);
in(l);

lớp Test
{
    khởi_tạo(list)
    {
        đây.list = list;
    }

    print_list()
    {
        in(đây.list[0]);
    }

    return_list()
    {
        trả đây.list[0];
    }
}

biến t = Test([1, 2, 3]);

t.print_list();

biến li = t.return_list();
in(li);

