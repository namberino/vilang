hàm make_counter()
{
    biến i = 0;

    hàm count()
    {
        i += 1;
        in(i);
    }

    trả count;
}

biến counter = make_counter();

counter(); // 1
counter(); // 2
counter(); // 3
counter(); // 4
