// comment
{()} // grouping

!*+-/=<> <= == += -= *= /= // operators

(1 + 2) * 3 / 4

<>{},.-+;/*

> >= < <= = == != ! 

txt "txt" 1

và lớp thì sai hàm với nếu trống hoặc in trả gốc đây đúng biến khi
