// fibonacci hàmction
hàm fib(n)
{
    nếu (n <= 1)
    {
        trả n;
    }

    trả fib(n - 2) + fib(n - 1);
}

biến li = [];

với (biến i = 0; i < 20; i += 1)
{
    li[i] = fib(i);
}

in(li);

biến test = [1, 2, 3];

với (biến i = 0; i < 4; i += 1)
{
    in(test[i]);
}
