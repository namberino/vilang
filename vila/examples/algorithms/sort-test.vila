nhập "lõi:sort";

biến array = [1, 9, 2, 4, 3, 6, 5, 7, 8];
bubble_sort(array);
in("Bubble sort:");
in(array);
in("");

biến array = [1, 9, 2, 4, 3, 6, 5, 7, 8];
insertion_sort(array);
in("Insertion sort:");
in(array);
in("");

biến array = [1, 9, 2, 4, 3, 6, 5, 7, 8];
quick_sort(array, 0, độ_dài(array) - 1);
in("Quick sort:");
in(array);
in("");

biến array = [1, 9, 2, 4, 3, 6, 5, 7, 8];
selection_sort(array);
in("Selection sort:");
in(array);
in("");
